{% extends 'base.html.twig' %}

{% block title %}Modifier un évènement - WeFrip'{% endblock %}

{% block body %}
<div class="container mb-3">
    <h1 class="text-center m-3">Modifier l'évènement </h1>
    <h2 class="text-center">{{ event.eventName }} créé par {{ event.creator }}</h2>

    <div class="d-flex justify-content-between">
        <a href="{{ path('app_map') }}" class="btn bouton mb-3"><i data-feather="arrow-left"></i> Revenir sur la carte</a>
        {{ include('map/_delete_form.html.twig', {'button_label': 'Supprimer'}) }}
    </div>
    
    {{ include('map/_form.html.twig', {'button_label': 'Modifier'}) }}
</div>
{% endblock %}
